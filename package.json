{
  "name": "finmark-milestone2",
  "version": "1.0.0",
  "private": true,
  "description": "FinMark Milestone 2 - root dev orchestration",
  "scripts": {
    "db": "docker compose up -d",
    "dev": "npm run db && concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
